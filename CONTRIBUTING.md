# 기여 가이드

음메 저장소에 코드를 넣는 방법을 안내한다. 품질 검사와 리뷰 기준의 상세는 아래 문서를 따른다.

- [팀 약속](docs/00_team-agreement.md)
- [CI 파이프라인](docs/01_ci-pipeline.md)
- [코드 품질 유지](docs/02_maintaining_code_quality.md)

<!-- 문서 담당: 프로젝트 한 줄 소개, 이 문서를 누가 읽는지(팀원 / 외부)를 여기에 보강한다. -->

---

## 1. 준비

1. Python 3.12 이상 설치
2. 저장소 클론
3. `pip install -r requirements.txt`
4. `python main.py`로 실행 확인

<!-- 문서 담당: OS별 설치, 가상환경, 실행 화면이 안 뜰 때 점검을 덧붙인다. -->

---

## 2. 작업 흐름

`main`에 직접 커밋하지 않는다. 작성자는 자신의 PR을 단독 승인하지 않는다.

1. 작업 전 이슈 등록 (`[버그]`, `[기능]`, `[질문]`)
2. `main`에서 브랜치 생성 (`feat/…`, `fix/…`, `refactor/…`)
3. 구현 후 브랜치에서 실행 확인
4. 단일 PR 템플릿에 변경 종류를 표시하고 이슈를 연결한다 (`Fixes #` / `Closes #`)
5. CI 통과 후 리뷰·품질 담당이 검토
6. 승인되면 개발 리드가 `main`에 병합

공통 파일(`main.py` 등)을 바꿀 때는 이슈 댓글로 먼저 알린다.

<!-- 문서 담당: 스크린샷 또는 브랜치/PR 생성 절차를 팀원이 따라 할 수 있게 적는다. 하루 작업 끝 Push 등 팀 약속 3절을 여기와 맞춘다. -->

---

## 3. 이슈와 풀 리퀘스트

빈 이슈·빈 PR은 열지 않는다.

이슈는 종류별로 고르고, PR은 템플릿 하나를 쓴다. 버그면 `Fixes #`, 기능이면 `Closes #`로 이슈를 연결한다. 질문은 PR을 열지 않는다.

PR에는 요약, 종류, 변경 내용, 확인 방법, 테스트 계획을 적는다.

이슈 템플릿: `.github/ISSUE_TEMPLATE/` · PR 템플릿: `.github/pull_request_template.md`

<!-- 문서 담당: 템플릿을 참고해서 어떻게 이슈와 풀리퀘스트를 올려야하는지 정한다. -->

---

## 4. 커밋 메시지

```
타입(범위): 변경 요약
```

타입: `feat` / `fix` / `refactor` / `chore` / `docs` / `test`  
범위: `list` / `roulette` / `result` / `effects` / `common`

브랜치 종류, 커밋 타입, PR 템플릿은 서로 맞춘다. 예시는 코드 품질 유지 문서 3절을 본다.

<!-- 문서 담당: 좋은 예·나쁜 예를 더 넣고, 이슈 번호를 본문에 쓰는 방식을 정한다. -->

---

## 5. 리뷰와 CI

- 리뷰는 48시간 이내를 목표로 한다
- 리뷰어는 재현, 인접 기능, 예외 상황, 실행 가능 여부를 본다
- GitHub Actions(`ci.yml`)가 린트·테스트를 수행한다
- CI가 실패하면 수정한 뒤 리뷰를 끝낸다
- `main` 병합은 CI 통과와 리뷰 승인을 전제로 한다

<!-- 문서 담당: 리뷰어 지정 방법, 수정 요청 후 재요청 절차를 적는다. CI 빨간불 로그 보는 법은 01번과 중복되지 않게 짧게만 적는다. -->

---

## 6. 코드 규칙

- PEP 8, 기능마다 파일 1개, 파일 입출력은 `encoding="utf-8"`
- 새 라이브러리는 `requirements.txt`에 추가한다
- 토큰, 비밀번호, 개인정보는 올리지 않는다
- 로컬 전용 경로에 의존하지 않는다

<!-- 문서 담당: 디렉터리 구조, 함수·모듈 이름, 공통 코드를 어디에 두는지 개발 리드와 맞춰 적는다. -->

---

## 7. 외부 기여자

1. `good first issue`에 댓글로 배정을 요청하고, 지정된 뒤 시작한다
2. 저장소를 Fork한 뒤 브랜치에서 작업한다
3. 이 저장소 `main`을 base로 PR을 연다
4. 팀원과 동일한 이슈·PR 템플릿, 커밋 규칙, 리뷰 기한(48시간)을 따른다

<!-- 문서 담당: Fork·PR 화면 경로, 라벨 목록이 정해지면 여기를 갱신한다. -->

---

## 8. 행동 수칙과 AI 도구

- 참여자는 `CODE_OF_CONDUCT.md`를 따른다
- AI 도구는 사용할 수 있다. 직접 실행해 확인한 코드만 올리고, PR에 사용 여부를 적는다

<!-- 문서 담당: CODE_OF_CONDUCT가 없으면 링크를 빼거나 파일을 만든다. AI 사용 문구 예시를 한 줄 넣는다. -->
